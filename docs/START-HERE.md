# START HERE — agent-os

> New session (human or AI)? Read this first. It's the fastest path to *"I know what's going on."*
> You should **not** need to re-read the whole repo to be productive — this doc + `DECISIONS.md` are the state.

## What this project is
Jarod's own **Agent OS**: a local-first personal control plane that **acts** on his AI stack — see + control all agents / skills / tools / context in one place — with a shared **memory/brain** for all his agents. Greenfield, for his own daily use. Inspired by, **not** a port of, a studied agent-OS template (distilled in local notes, kept out of the repo).

## Intent anchor (north star, working form)
> A local Agent OS that **acts** on my stack — one place to see and control all my agents, skills, tools, and context — with a shared **memory/brain** as the piece I most want.

**Canonical North Star: [`STRATEGY.md`](../STRATEGY.md)** (formalized 2026-07-01). The quote above is the working-form restatement.

## The vision so far
**Three buckets of functionality** — all in scope, none cut:
- **A. Observe + Control** — see every agent/skill/tool/context across runtimes, and *act* on them (install / toggle / run / hand off). The "acts, not observes" core.
- **B. Daily prescription** — a Dream-style audit that prescribes *and runs* the highest-leverage actions.
- **C. The Agent Brain** — a shared 4-layer memory across all agents (Jarod's headline interest — see `MEMORY-SYSTEM-VISION.md`).

**The architecture that serves all buckets** (this *is* "architecting around the north star"):
- **Seam #1 — typed data contract** (one zod schema, producer + consumer).
- **Seam #2 — runtime/agent abstraction** (one adapter interface every runtime implements).
- **Shared-state substrate** — a local **MCP Gateway** (all runtimes point at it; hosts the brain-as-MCP-server) + a **Task Board** (blackboard coordination). The brain = shared state behind the gateway.

**Scope decided: "Rung 2"** — a *local* control plane on one machine, but with those two seams built **remote-ready**, so driving agents on other machines / a VPS later ("Rung 3") is an *extension*, not a rewrite.

## Where we are RIGHT NOW

> **Continuity record = the ▶ NEXT below (decision #8 as amended — #51).** agent-os has no always-on brain server yet (that arrives at **U15** — the normal state, not a degradation), so `START-HERE.md`'s ▶ NEXT *is* the one continuity record — not a lagging projection of a machine cursor. Crash-safety between handoffs is the Stop-hook breadcrumb (`.claude/session-breadcrumb.md`), surfaced by the SessionStart resume-check. At U15 the substrate `write_handoff` becomes the machine record (written first) and this ▶ NEXT becomes its agreeing projection. **No "substrate not written" apology — there is simply no substrate yet, by design.**

- ✅ **Understand + Design + Migrate + `ce-strategy`** (2026-07-01) — the reference study distilled, feature slate + memory vision written, North Star canonical in [`STRATEGY.md`](../STRATEGY.md).
- ✅ **`ce-brainstorm`** (2026-07-01) — slice 1 scoped: the **"parity-enabling cut"** (Brain/board substrate seed, agents-primary + 4-runtime inventory + parity actions). Decisions #9 (slices sequence, never shrink) and #10 logged.
- ✅ **`ce-doc-review` ×2** — requirements hardened by a 6-reviewer panel (8 fixes), plan hardened by a 5-reviewer round-2 panel (11 findings walked through and applied: capture-time redaction, token model, in-process capture, session-keyed handoffs, U13/U14/U15).
- ✅ **`ce-plan`** (2026-07-01) — **implementation-ready plan: [`docs/plans/2026-07-01-001-feat-slice-1-substrate-parity-plan.md`](plans/2026-07-01-001-feat-slice-1-substrate-parity-plan.md)**. Forks closed by decision #11: thin vertical slice · Claude Code + Codex writes · **Brain seed is the v1 flagship**.
- ✅ **Whole-system architecture pass** (2026-07-02, decisions #12–13) — closed a mid-`ce-work` gap: no consolidated whole-system view existed, so execution kept wobbling (3 step-backs in one session). Produced the durable cohesion map → **[`ARCHITECTURE.md`](ARCHITECTURE.md)** (Jarod affirmed the exit condition). Adopted **4 extensibility hooks** (per-run cost/tokens, Event Spine, `Inferred<T>` provenance, Brain redaction choke-point) + **3 reserved homes** (Notifications, Economics, Scheduling). **Roster updated:** OpenClaw dropped; real set = Claude · Codex · Hermes · Cursor · Antigravity · OpenCode. **Runtime = Bun confirmed**; FE framework deferred to U11 (lean: React Router 7 / Remix, Tailwind + Shadcn, colorblind-safe). Ideas mined from the studied template → local notes (kept out of the repo).
- ✅ **`ce-work` begun — U13 gate GO + U1 shipped** (2026-07-02). U13 test #2 validated the sharpest assumption (raw-trail resume-sufficiency): a *cold* agent reconstructed in-flight state + the exact next action from a mechanically-extracted trail alone (decision #14). Test #1 (injection) was skipped — mechanism already de-risked, cadre confounded. **U1 committed** (`2bd3a14`): Bun scaffold + typed contract core (seam #1) — all 5 records + all 4 hooks (#13); **30 tests green, tsc clean**. Stack + roster locked (decision #15).
- ✅ **Slice-1 substrate MERGED to `main`** (PR #1, 2026-07-04; merge `f0c7727`). U1 typed contract (seam #1) **+ U14 config-write discipline engine** (`src/configwrite/*`): byte-exact backup → merge-don't-clobber → write-to-temp + atomic rename → **identity-checked** undo journal; format-aware (JSON/TOML/YAML); backups `0600` under the OS data dir (KTD6 / R11). Full review loop ran — `ce-work` → `ce-simplify-code` → `ce-code-review` → **3 Codex adversarial passes** → Copilot (folded in, `68826ba`). U1 contract hardened alongside: `maxSensitivity` redaction escalation, **`WorkState` as a discriminated union** so its resume-state invariant holds at the exported JSON-Schema boundary (not just zod parse), `strictObject` records. **65 tests green, `tsc` clean.** Two config-write robustness findings **deferred** (decision #16) — now tracked as `deferred` issues.
- ✅ **U2 — store + persistence MERGED** (PR #3, 2026-07-04; merge `71c123d`). `src/store/*`: 6 Drizzle tables on `bun:sqlite` (WAL), **async repo interface** (Turso swap point, KTD3), `readWorkState` derives the `WorkState` discriminated union (AE1/AE2), self-recording access-log + `hitRate` (an explicit primitive, NOT auto-logged). Full loop + bot review. 5 residuals tracked (open-findings U2-R1…R6); **U2-R1 is a live instruction for U4**: validate external HTTP/MCP writes at the repo boundary via the contract schema.
- ✅ **U3 — server spine + security COMPLETE, in PR #4** (awaiting bot re-review + merge; HEAD `e524ebf`). `src/server/*` + `src/paths.ts`: one Bun+Hono process; `securityGate` = loopback (socket-peer) + Host allowlist + per-boot CSPRNG token (timing-safe); **fail-closed routing** (gate default-on via `use("*")`, `/health` the one exemption); loopback-only `127.0.0.1` bind; `0700` data dir; bind-then-write + atomic token; dataDir single-instance lock. Redaction + `/work-state` **deferred to U4** (decision #17). Loop ran: ce-work → simplify → ce-code-review → **3 autonomous Codex adversarial passes** (found fail-closed routing, token-clobber, single-instance — all folded). **125 tests green, tsc clean.** Single-instance `flock` hardening deferred to U15 (decision #18, U3-R3).
- ✅ **U4 — Brain MCP server + tools + redaction MERGED** (PR #6, merge `16aae66`). The flagship payoff — **the substrate is agent-consumable.** `src/redact/*` (the schema-driven redaction choke-point, KTD2 — masks at/above `secret`, escalates per-record via `maxSensitivity`, fail-closed, shape-preserving), `src/workstate/*` (the ONE shared `read→redact→present→log` path — the `read_work_state` tool and `GET /work-state` route are byte-identical by construction), `src/mcp/*` (`@modelcontextprotocol/sdk@1.29` WebStandard transport, transport-per-session on `mcp-session-id`, 3 tools), all behind the U3 gate. **Identity model (decision #19):** sessionId = transport session id (KTD8); machineId = a **persisted opaque UUID** (not `os.hostname()` — bot review caught that it bakes the user's name into every record); source = validated self-declared input. **Resolved U2-R1/R5/R6.** Full loop: ce-work → ce-simplify → **ce-code-review (5 personas — security CLEAN BILL: no secret escapes any read path)** → **Codex adversarial (cross-model — caught a session-cap TOCTOU + drove the pager to keyset `(ts,id)`)** → CodeRabbit/Copilot (folded: machineId PII → opaque id, array-mask shape, machine-id validation). **164 tests green, tsc clean.** Deferred: **U4-R1** (hitRate mixes session-id producers — DORMANT, `/status` discards the value).
- ✅ **U5 — Claude Code breadcrumb capture (raw lane) MERGED** (PR #8, merge `dc16e10`; decision #20). `src/capture/tailer.ts` (harness-agnostic byte-offset tailer — Codex/U8 reuse it via the injected `Extractor`) + `src/capture/claude-code.ts` (the ONE module holding the undocumented JSONL shape). **Sufficiency gate = GO:** the spike (5 messy mid-implementation cuts, closed-book, opus — full report `docs/spikes/2026-07-05-u5-resume-sufficiency.md`) validated the core bet AND set the breadth: the trail recovers *direction* but not *observations*, so U5 also captures a terse **`toolUseResult` digest** (test pass/fail, errors, subagent verdict — structural, survives crashed/terse sessions). Capture-time secret classifier (KTD2) feeds the U4 redactor; breadcrumb `sessionId` = CC transcript id (U4-R1 documented, deferred). Full loop: ce-work → ce-simplify → ce-code-review (correctness/security/adversarial/testing — **security caught a real classifier boundary bug**: `\b` misses `DATABASE_PASSWORD=`) → **Codex cross-model gate ×2** (caught a no-lost-crumbs seam violation; the in-place-rewrite residual was **overridden as a scoped defer to U8** — CC is append-only, so the invariant is narrowed to "append-only inputs"). **229 tests green, tsc clean.** Deferred: **U5-R1** (uncapped cold-start read), **U5-R2** (rewrite safety → U8).
- ✅ **Repo went public + housekeeping** (2026-07-05, decision #22) — scrubbed ALL studied-template association (name + `docs/reference/` + FEATURE-SLATE gone from tree **and** history), pushed to a fresh public **[`jarodtaylor/agent-os`](https://github.com/jarodtaylor/agent-os)**, archived the pre-public repo private (`agents-os-internal` — PRs + issues preserved), added the README front door, migrated the 17 open issues (roadmap + deferred) to the public tracker. **Zero-association verified across tree + history + remote.**
- ✅ **U3-R3 flock lock SHIPPED + MERGED** (PR #19, 2026-07-06) — the pid-file single-instance guard is now a **real OS advisory lock**: `flock(fd, LOCK_EX|LOCK_NB)` bound from libc via Bun FFI (`src/server/single-instance.ts`), held on an fd for the process lifetime so it auto-releases on exit (clean OR crashed) — closing the two pid-file edges (stale-reclaim TOCTOU, pid-recycle) that **gated U6**. Held-fd + **no-unlink-on-release** (inode-divergence two-writer trap), lazy+guarded FFI bind (no import-time throw), fail-closed with distinct bind-vs-contention errors, idempotent release. Full loop: advisor → ce-simplify → ce-code-review (4 personas, **no P0/P1**) → **Codex cross-model gate** (no-ship on one medium finding → **partial-adopt logged loudly**: message-honesty fix now, **errno-awareness deferred** → decision #24 + a `deferred` errno issue, trigger = first VPS/container run / network-FS data dir / observed misdiagnosis) → bot review (Copilot await-exit + CodeRabbit fail-closed bind test, both folded). Acceptance = a cross-process **crash-release test** (child acquires → SIGKILL → parent re-acquires proves OS auto-release). **229 tests green, tsc clean.** Decisions **#23** (scope: flock-only pulled ahead of U6; launchd stays post-U6) + **#24**. **Learning compounded** → `docs/solutions/tooling-decisions/os-file-locking-in-bun-via-ffi.md` (first `docs/solutions/` doc). U3-R3 issue auto-closed on merge.
- ✅ **U6 — Claude Code consumption + clean-end hooks SHIPPED + MERGED** (PR #23, 2026-07-08; merge `3309f59`; decision #26). **The felt checkpoint is BUILT:** a fresh CC session picks up work-state with nothing pasted (R1/KTD5). `hooks/session-start.ts` (token read at call time → gated `GET /work-state` → `hookSpecificOutput.additionalContext`; **fail-open** — every failure emits nothing + exits 0), `hooks/session-end.ts` (graceful-end marker via the new gated **`POST /session-end`** — KTD9: server stays sole writer, hook never opens the DB), `hooks/mcp-headers.ts` (`headersHelper` reads the token fresh per MCP connection — KTD6 delivery to CC's native client), `src/install/claude-code.ts` (two files — `~/.claude/settings.json` hooks + `~/.claude.json` MCP — via the U14 engine, **cross-file transactional**); `resolvePort`/`TOKEN_HEADER` → `src/paths.ts` (single-source). Full loop: ce-work → ce-simplify (5) → **ce-code-review (10 personas + the redaction-boundary-reviewer — CLEAN BILL, 0 P0/P1; caught the uninstall whole-file-restore bug + a fail-open hole)** → **Codex gate** (fixed #2 install-transactionality; **#1/#3 scoped-deferred, Option B, logged loudly**). Bot feedback folded (fail-open hardening: bounded stdin read + total `formatAdditionalContext`). **270 tests green, tsc clean.** Learning compounded → `docs/solutions/design-patterns/building-installers-on-the-config-write-engine.md`. Deferred: **#21** (U14 subtree replace/remove primitive) + **#22** (co-located-hook granularity). **✅ Live VS1 LIVED** (2026-07-08) — the installer ran against Jarod's real `~/.claude` (5 existing SessionStart hooks + 3 MCP servers preserved), the SessionStart hook injected the seeded handoff end-to-end; **uninstalled back to pristine after** (re-install for keeps at U15 launchd, when an always-on server exists).
- ✅ **U7 — `/handoff` rewired substrate-FIRST SHIPPED** (2026-07-08, decision #27). `.claude/skills/handoff/SKILL.md` now calls `write_handoff` (brain MCP) as **Step 3, before any doc write** → the substrate cursor `{next, lastDecided, inFlight}` keyed `(project, sessionId)` is the ONE machine-readable continuity record; START-HERE ▶ NEXT + `HANDOFF.local.md` are **human projections that must agree**; DECISIONS.md is the ledger (not a competing resume-record); substrate-unreachable → docs-only + **mandatory staleness flag**. **VS3 proven** (one-record upsert + fallback reachable + projection-agreement). **Git-tracking finding:** `.claude/` is gitignored → the skill is **local-only/untracked** (no PR); tracked deliverable = the DECISIONS/START-HERE flip, direct to `main`. This wrap **dogfooded** it. 270 tests green, tsc clean. **⟳ Record-model AMENDED 2026-07-20 (decision #51):** substrate-FIRST was U7's shipped form; the record's *home* is now **substrate-OPTIONAL** — today START-HERE ▶ NEXT IS the record (no always-on substrate yet), the substrate becomes primary at U15. The "one record, docs agree" invariant is unchanged; the every-handoff staleness apology is retired.
- ✅ **U8 — Codex integration SHIPPED + MERGED** (PR #25, 2026-07-09; merge `aa13a24`; decisions #29–30). **The 2nd harness is on the shared Brain substrate:** dual-token gate + a **stable, live-revocable Codex credential** (`codex.token`, 0600, read fresh per request), Codex rollout capture (`src/capture/codex.ts` reusing the tailer via the injected `Extractor`), installer (`~/.codex/config.toml` MCP reg + `hooks.json` SessionStart + `AGENTS.md` pointer + injection hook) all through the U14 engine — now with an opt-in **`targetMode`** that publishes secret-bearing configs `0600`. **The credential grind (Codex gate ×5 this session) → two CEO rulings:** **#29** — the two-location token model's lifecycle-**race** class is retired via **single-source** (issue #24); **#30** — after the gate kept surfacing installer edges (publish-window + diverged-hooks leftover **FIXED**; uninstall-idempotency + targetMode-no-op **DEFERRED**), the non-converging trend hit #29's readiness boundary → **ship now + do #21 next**. Full loop ran (ce-work → ce-simplify → ce-code-review → gate ×5 → ce-compound → CodeRabbit/Copilot folded in `633a974`). Learning compounded → `docs/solutions/architecture-patterns/cross-harness-credential-second-location-race-generator.md`. **338 tests green, tsc clean.** Deferred → issues **#21** (targeted-removal, now the next unit), **#24** (single-source), **#26** (targetMode no-op). **Live behavioral acceptance still pending** (post-U15, like U6 VS1): a real Codex session lists+CALLS `read_work_state`; verify Codex passes `cwd` in the hook stdin; VS6 per-harness hit rate.
- ✅ **Substrate caught up** (2026-07-09) — server booted manually, the stale handoff re-written via `write_handoff` (MCP-over-HTTP; the brain MCP wasn't registered this session). Verified round-trip: `GET /work-state` returns the record `fresh`, cursor agrees with ▶ NEXT below, `inFlight` redacts. Server stays manual-start until U15 (launchd).
- ✅ **Realignment — the human/product layer** (2026-07-09, decision #31). Jarod: docs were agent-only ("WTF did we build?"), "Brain" named two things, roadmap invisible → **`docs/PRODUCT.md`** (stakeholder-grade projection: TL;DR · 4 tracks · what-works-today · release map with **committed/candidate/vision** labels + per-release proof lines · glossary · **U-number decoder ring**; committed `466cbdb`) + the refreshed private visual artifact (same URL) + `/handoff` Step 4 now maintains both, **evidence-gated** (status flips need merged-PR/lived-check evidence; roadmap changes need a DECISIONS row). Dual-reviewed: same-model gap pass (12 findings folded) + **cross-model `codex exec` gpt-5.6-sol** (7 findings; 6 folded, 1 → interview Q2). Advisor tool confirmed broken for fable/fable → fallback protocol memorized (cold-read fable subagent and/or codex exec). **#21 paused for the interview.**
- ✅ **Alignment interview COMPLETE + STRATEGY amended** (2026-07-10, decisions #32–#41; strategy commit `c0da0dc`). Postures locked: **naming split** — substrate = built, Brain = memory system only, code names unchanged (#32) · **routing quality over utilization, operator-directed** (#33) · **propose-first dispatch + audit** (#34) · **agent-written Brain + the AGENT-FIRST principle** (#35 → vision note #11) · **auth = status + native-login launch, zero custody** (#36) · **Coordination proof = one non-coding workflow** (#37) · **phase order: Memory → Coordination** (#38) · **breadth open-ended — lists never bound scope** (#39) · **Grok Build waits for real rotation** (#40) · **ClaudeOS v2.13 walkthrough = dedicated session post-#21** (#41). Two-tier communication contract ratified (#32; memorized). Memory-system DESIGN deliberately not started — it gets its own 2–3 sessions (#35 scope guard, #38).
- ✅ **U14-ext (#21) MERGED** (PR #34, merge `891bedc`, 2026-07-11; decision **#42**). The config-write engine's targeted key/array removal + **presence-semantics pass** (closed #29/#30): three-outcome `statTarget` (regular file / ENOENT-absent / everything-else-THROWS incl. FIFOs+dirs) + `hasOwn`-exact leaves + exported `MERGE_NOOP` abstain → an absent-target removal is a TRUE no-op on foreign-formatted live configs; both uninstallers targeted-removal with `UninstallOutcome {removed, failed, warnings}` + typed `AppliedButUnjournaledError`; **never-loosen mode policy** (gate-proven: restoring looser modes can expose secrets added under 0600 → #33); aliased-YAML removal fails closed (CoW → #32). Full loop: ce-work → simplify → 9-persona review → **8 completed codex gate runs** (2 lesson-#3 stops honored: the presence design pass + the mode revert) → CodeRabbit/Copilot folded (incl. the statTarget-isFile FIFO-block catch). **338→417 tests green, tsc clean on main.** Learning compounded → `docs/solutions/architecture-patterns/presence-semantics-not-byte-level-noop-checks.md`; installers doc refreshed to shipped reality. **Merged with #31 OPEN by CEO call** (moved-checkout hook identity — pre-existing U6/U8 model); other defers: #27/#28/#32/#33. **#24 now unblocked.**
- ✅ **2nd reference adopted + reference consolidated + teardown reshaped → the feature catalog** (2026-07-15, **decision #43**). Jarod stopped the walkthrough to bring **Julian Goldie's Agent OS** into the fold first — correctly, and the finding is the headline: **ClaudeOS observes; Goldie's ACTS.** Verified at the app level: ~57K LOC, **241 API routes** that spawn processes + write files. It's the only prior art we have for our own north star, so dispositioning the slate with ClaudeOS alone would have judged "acts, not observes" against half the evidence. **Per-feature truth lives in the catalog, not here** — my opening seam claims went **2-for-4** once checked against code: the **MCP catalog is Hermes-only, NOT gateway prior art** (it's the per-agent registration our gateway replaces), the **"three runtime adapters" are six-plus copy-pasted shells** (an anti-pattern aimed at our modularity principle), `ultracodeRuns` as a per-run cost log **confirmed**, and the task-board pairing was right *by luck*. **Name-adjacency is not evidence in these repos** — that's why the catalog has a `Real?` column, and it's earning its keep (Goldie's "Reviewer agent" is `html.length > 120` + a cosmetic 950ms delay; OpenMontage spawns a python script that isn't in the pack; ClaudeOS's activity log unconditionally zeroes its own data; its model-leaderboard API built for agents to query has **zero callers**). **Two findings that change our plans:** (1) **neither reference has MCP-gateway prior art** — the Gateway is ours to invent, no shoulders to stand on; (2) **Goldie's "cross-agent memory" doesn't do the hard part** — the only callers of its memory-log are its *own UI* passing an agent's name, so a Claude Code session in a terminal writes nothing to it. Ours has agents writing directly over MCP from anywhere. That's independent evidence for **#35 (agent-written Brain)**. **What Goldie genuinely holds that we lack:** a SQLite dispatcher-backed board (promote/reclaim/assignee-gating; engine ships in an external pip package, so not independently provable here), **Agent Room** (agents visibly handing off to each other), an **Idea Pipeline** implementing **#34's propose-first single gate** with two independent correction loops, and a **Loop engine** that verifies builds in real headless Chromium. **Consolidated:** all reference material now lives in the gitignored root **`reference/`** (`docs/` = our state, `reference/` = their stuff) — `reference/README.md` is the guide, each app has a `CLAUDE.md`, `docs/reference/` is emptied but **stays ignored as a deliberate guard** (#22's scrub cost a fresh repo + history rewrite; #22/#41 still cite the old path). Killed a duplicated `GRAPH_REPORT.md`; **preserved `reference/claude-os/mined-ideas.md`** (the source of the 4 adopted hooks per #12–13 — a near-miss: a gitignore-respecting `grep` hid it from the pointer sweep). **Reshaped** per Jarod's framing — *"outcomes and features these repos claim to do, not the directions on how"* — so the deliverable is **`reference/FEATURE-CATALOG.md`** (local-only, gitignored): ONE list across both apps, grouped by the 4 tracks, every row an outcome-you-could-plug-in, with a **`Real?`** column (claims checked against code — both are community file-drops with live demo-ware risk) and a **blank `Call` column that is Jarod's alone**. Chain: **Call → wishlist → feasibility → prioritized roadmap.** Scope = shallow-all + relevance-graded, reconciling **#39** (every vertical gets a row, incl. the ~60% that's Julian's SEO/content business — nothing unseen, every "skip" overrulable).
- ✅ **Catalog coverage = all 5 passes** (34 guides + un-guided route sweep + the ClaudeOS teardown). **Near-miss worth remembering:** I briefly shipped it labelled "all five folded in" when the guides 0–8 pass hadn't returned — my self-check counted rows/links/blank-cells, which structurally **cannot detect a pass that never arrived**. The advisor caught it; the pass then landed late (it was slow, not dead) and is now integrated. **Lesson: verify coverage against what was *assigned*, not just internal consistency of what was *assembled*.** Same family as the name-adjacency error — both are "I checked the thing in front of me instead of the thing I claimed."
- ✅ **Foundation-first confirmed; feature dispositioning deferred; stage-manager MVP vision captured** (2026-07-16, **decision #44**). Jarod's call after skimming the catalog: **don't stop to blindly annotate 93 rows — finish the in-flight foundation first** (Kia before Ferrari; he won't know which features he wants until he's using a real MVP). His test — "does any remaining foundational unit change based on a feature-catalog choice?" — answered **NO**: **U9** (Observe half) · **U10** (Act half) · **U15** (always-on) are feature-agnostic mechanism, built as the extensibility spine (#12–13) every future feature rides. Only trims: **U12 drops OpenClaw** (off-roster #15/#40); **U11 (the view) is where the MVP crystallizes** = the deferred MVP-definition session. The **feature catalog (`reference/FEATURE-CATALOG.md`, local-only) is now a mined-later backlog, not a gate.** Jarod's **"stage-manager" MVP framing** (agent-os is NOT a harness; it **provisions** skills/memory/personas/hooks/plugins INTO the harnesses so they work **with OR without** agent-os; SDLC spread across harnesses — Architect→Execute→QA→Review→PR, each a possibly-different harness; provisioning-before-orchestration; dogfood via Herdr) → **captured in [`docs/STAGE-MANAGER-VISION.md`](STAGE-MANAGER-VISION.md), deferred to the MVP-definition session.** It **validates + extends** the foundation, does not impede it. Framing correction logged: **observe is co-equal with act** (north star = see AND control); features tag observe / act / observe-then-act.
- ✅ **U9 — inventory scanners SHIPPED + MERGED** (PR #38, merge `d16cf6c`, 2026-07-16; decision **#45**). The **Observe half**: one typed inventory across the slice-1 roster, **Claude Code + Codex only** (each further harness is a one-line registry row when it enters real rotation per #40). `src/scan/*`: pure DI scanners, no persistence (rescan = live disk, R8/AE5), fail-soft readers (stat-guarded regular-file reads, 16 MiB bound, path-only warns), crash-safe composition (a throwing scanner degrades only its own runtime — and the backstop catch is **zero-inspection**: binds nothing, reads nothing off the throw). Full loop + **5 codex-gate rounds** — rounds 2–4 no-shipped on the error-logging surface until the fix became structural and the **threat model was written down (decision #45**, folded into the gate's local reference so every future run is scoped); **round 5 = approve, no material findings** — the first run carrying the written scope. CodeRabbit + Copilot folded. **444 tests green, tsc clean; live scan 46 items.** Learning compounded → `docs/solutions/conventions/an-unwritten-threat-model-is-why-the-adversarial-gate-loops.md`; **`CONCEPTS.md` seeded** (build-loop + inventory vocabulary). Scoped defers: **#35** (per-project axis → U11) · **#36** (Codex skill discovery) · **#37** (structured degradation metadata → U10/U11).
- ✅ **Dogfood run 1 COMPLETE — the multi-harness SDLC loop is mechanically PROVEN** (2026-07-16→20, decision **#48**; `agent-cost-tracker` PR #1 merged, 14-entry `FRICTION.md`). The full loop ran via Herdr: Claude plan → Codex execute → Claude review → Cursor 4-specialist QA gate → PR → real bot-review cycle → Jarod merged. **Entry point: [`agent-cost-tracker/docs/runs/run-1/RETROSPECTIVE.md`](https://github.com/jarodtaylor/agent-cost-tracker/blob/main/docs/runs/run-1/RETROSPECTIVE.md)** — it cascades to everything (workflow SSOT, friction log, observations, `PROVISIONING.md`). Headlines: **P0 = harness command-execution authorization** (the loop couldn't run until Cursor's per-command approvals were bypassed via human-authorized `--force`) + **structured cross-harness contracts** (the QA verdict nearly mis-parsed); Codex autonomously spawned 4 subagents **all at Sol/xhigh** (~3× output cost on the simplify panel → model-routing is evidence-backed); **`PROVISIONING.md` = U10's spec** (copy/compose/new/transform, non-1:1 map). NOT tested: output quality, multi-run continuity, harness swapping — n=1 mechanical. Compounded → the verify-harness-surfaces solutions doc (false-absence example added).
- ✅ **U24 — single-source Codex credential SHIPPED + MERGED** (PR #41, merge `033c1d2`, 2026-07-20; decision **#49**). Retired the second-location `codex.token`: the gate reads the stable credential per-request straight out of `~/.codex/config.toml` (the bytes Codex sends) via the new `src/codex-credential.ts` — a pure/total `extractCodexToken` + fail-closed `readCodexToken` shared by the gate **AND** uninstall verification (one reader → "accepts" and "proved-gone" can't drift); installer **reuses-not-rotates**; revocation **inverts** (config strip fails loud, no second file to backstop it); deleted `codexTokenPath`/`resolveCodexToken`/`revokeMintedToken`/`tokenPreexisted`. Resolves the **#29** reconsider trigger. Full loop → **ce-code-review (8 reviewers + cross-model Codex peer)** → **Codex adversarial gate 4 ROUNDS to APPROVE** — each round caught my code as the **outlier vs an invariant the codebase already holds** (legacy-file cleanup → lstat/symlink discipline → bounded-read/no-hang guard), all folded; two conditional TOCTOUs correctly **deferred**, not patched. **Review-phase design change: dropped the mtime cache** (gate reads fresh per request) to close a same-tick revocation fail-open window on coarse-mtime FS (VECTOR #1). CodeRabbit + Copilot folded (stale "mtime-cached" wording). **481 tests green, tsc clean.** Learning compounded → `docs/solutions/architecture-patterns/one-pure-extractor-per-caller-failure-policy.md`. Scoped defers (filed): **#39** (reinstall-rotation TOCTOU → cluster #28/#29) · **#40** (failed-install stranded credential under concurrent Codex rewrite → **deferred to the decision-#16 config-write cluster, option A, 2026-07-20** — engine-level rollback fix belongs there, kept U24 subtractive).
- ✅ **SIDE QUEST — handoff HARDENED (Phases 1–2 DONE)** (2026-07-20, decision **#51**; plan [`docs/plans/2026-07-20-002-harden-handoff-port-from-agent-hud.md`](plans/2026-07-20-002-harden-handoff-port-from-agent-hud.md)). Ported agent-hud's crash-safety hooks (Stop `session-breadcrumb.sh` + SessionStart `session-resume-check.sh` → local-only in gitignored `.claude/`, both verified by direct run) and **reframed `/handoff` substrate-OPTIONAL**: START-HERE ▶ NEXT IS the record today, the substrate becomes primary at U15 — **retiring the every-handoff "substrate not written" apology** (the noise that triggered this side quest). `/handoff` also made **model-invocable** + given an **idempotence guard**; decision #8 amended (#51); the substrate-first solution doc reconciled. **This very handoff dogfoods the reframe — no apology emitted.** **Phase 3 (promote to `~/.claude`) deferred — later.**
- ✅ **U10 PLANNED — the provision engine is implementation-ready** (2026-07-20, decision **#52**; plan [`docs/plans/2026-07-20-003-feat-u10-provisioning-engine-plan.md`](plans/2026-07-20-003-feat-u10-provisioning-engine-plan.md)). One session ran the full pipeline: **ce-brainstorm** (Jarod's felt moment: scaffold/adopt a project and assign roles+harnesses+models per workflow; 4 forks locked — apply + **seed template** · **role-bundle manifest** (roles → harness + model + files, file-ops only) · **Cursor joins as the third target** (amends #45; its own trigger fired at run 1) · **CLI + propose-only MCP** (supersedes slice-1 KTD7)) → **interactive ce-doc-review** (6 personas; 9 findings folded — headline: the run-1 fixture alone would have shipped an overfit engine, secret hygiene made enforced-not-aspirational, `transform` type demoted to extension point) → **ce-plan enrichment** (7 units U1–U7 · 10 KTDs — opaque `text` format on the U14 engine, batch undo, semantic diff, shared front-gate with the secret classifier, no-lock scoped decision · the lived ceremony = acceptance fixture AE1 + generalization AE7; 3 research agents grounded it in verified code surfaces) → **headless round-2 review** (5 personas). **Naming ruling: the cross-harness run file is `RUN_STATE.md`** (was the dogfood's `HANDOFF.md`) — "handoff" now means ONLY session-to-session continuity. `CONCEPTS.md` gained the provisioning vocabulary. **⏸ Paused mid-`ce-plan` tail** — 7 round-2 findings pending application (1 P1: U7's `project`→blueprint-root resolution; 6 P2s), each with a committed recommendation in `docs/HANDOFF.local.md`.
- ⏳ **▶ NEXT — apply the 7 round-2 findings to the U10 plan** (open `docs/HANDOFF.local.md` — recommendations are pre-committed, don't re-derive; the P1 = a known-projects registry so U7's MCP tools never path-join a caller string) → **then `/unit-loop U1`** of the U10 plan (plan-local U1 = manifest contract + blueprint loader; U2/U3/U4 are parallel-eligible after it). **U15** (launchd always-on) follows; the **MVP-definition session** lands before/at **U11**. Deferred, unchanged: handoff **Phase 3** (→ `~/.claude`); the ClaudeOS V2.11/V2.13 delta walkthrough; the feature-catalog `Call` pass.

## Read next (in order)
1. `docs/plans/2026-07-01-001-feat-slice-1-substrate-parity-plan.md` — THE plan (scan headings: Goal Capsule → unit index → U13).
2. `docs/DECISIONS.md` — decisions 1–31 logged; 3 forks still open (deployment, remote adapter, memory routing).
3. `docs/PRODUCT.md` — the human/product map (Jarod-facing; TL;DR, release map, U decoder ring).
4. `docs/MEMORY-SYSTEM-VISION.md` — design grounding.
5. `docs/spikes/` — the validation spikes (e.g. the U5 resume-sufficiency test that gated the raw lane).

## Open threads
- ✅ **NotebookLM connected** (2026-07-01) — `notebooklm` CLI authenticated + skill installed (`~/.claude/skills/notebooklm`); 11 notebooks verified. Memory-relevant: **"AI Second Brain"** (`ba4b1af4…`), **"AgentOS"** (`0db2b38d…`), **"Claude, Hermes, and NotebookLM"** (`7d55c036…`). This unlocks the v1.1 Brain deepening + the memory-routing fork — pull the sources when the memory architecture phase starts (not during slice-1 build).
- **U12 spike** owns the non-Claude/Codex lane discovery — Hermes (9119 write surface) + Cursor/Antigravity/OpenCode as they come online — findings land in DECISIONS.md.
- **3 remaining deferred forks** (deployment model, first remote adapter, memory routing) — see `DECISIONS.md` open table.
- **Codex hit-rate** is the measured bet — U8 shipped BOTH the `AGENTS.md` pointer AND a SessionStart injection hook; the open question is *live consumption* (the pending VS6 acceptance: does a real Codex session list + CALL `read_work_state`?). If weak, tune the hook/pointer in v1.1.
- **Context-efficiency stack** (codebase-memory-mcp, graphify, the "90% token cut" tool/hook pattern) → these are gateway/Brain **consumers, not repo dependencies**: the **MCP Gateway** registers a server like codebase-memory-mcp *once* for all harnesses (its poster-child validation); **graphify** is the Brain L2 tooling (`MEMORY-SYSTEM-VISION.md` note #4). Wire in the **v1.1 gateway/memory phase** — build the seam first, don't bundle. The enforcement-via-hooks thesis ("one hook beats 1,000 words of CLAUDE.md") maps onto "act, don't observe" and directly informs **U6's** SessionStart/PreToolUse hook work.
- **Tracked residuals + roadmap → [GitHub Issues](https://github.com/jarodtaylor/agent-os/issues)** (decision #21): deferred findings carry the `deferred` label; upcoming units carry `roadmap`. **Skim the open issues at session start** (`tasks/open-findings.md` is retired, local-only). Still-hot: **#40** (failed-install stranded credential under concurrent Codex rewrite — **deferred to the #16 config-write cluster**, decided 2026-07-20) + **#39** (reinstall-rotation TOCTOU), both new from U24; **#26** (targetMode no-op — external-tamper-only, low); **U5-R1** (cold-start read) lands with the boot/supervision wiring — NOT U6 (hooks ≠ capture); the **flock errno-awareness** issue on first VPS/container run. ✅ Resolved: **U24** (PR #41 — single-source Codex credential, closed #24, retired `codex.token`); **#21** (U14 targeted key/array-removal, PR #34); **U8 Codex integration** (PR #25) — which also folded **U5-R2** + **#22**; the flock-lock (U3-R3, PR #19); the U6 felt-checkpoint **build** (PR #23) + **live VS1 LIVED**.
- **Repo is public: [`jarodtaylor/agent-os`](https://github.com/jarodtaylor/agent-os)** — a build-in-public reference/journey, **not** a supported OSS product. The pre-public history (PRs #1–9 + the GitHub issue tracker) lives in the private archive **`agents-os-internal`** (renamed 2026-07-05); internal/personal files (entry-point instructions, `tasks/`, `.claude/`, local reference notes) stay untracked + gitignored, kept local. A public **README** is the front door (next polish).
- **Codex adversarial review is autonomous now** — `~/.claude/scripts/codex-adversarial-review.sh` wraps the openai-codex companion (the slash command is `disable-model-invocation`). It's a required loop step; see [[jarod-compound-engineering-pipeline]] + `tasks/lessons.md`. **⚠️ Watch (learned U6):** switching codex accounts mid-session breaks the companion's auth (`"signed in to another account"`); a CLI re-login does NOT clear it — likely needs a fresh CC session. Sanctioned escape when it blocks a gate re-run on a trivial delta: explicit CEO Option-B marker advance. See memory [[claude-code-harness-quirks]].

## Continuity note
Migrated from an earlier local project on 2026-07-01. The original Claude Code session that produced all this may still be open as a fallback backstop. If anything here is unclear, that session is the deep backup — but this doc + `DECISIONS.md` should be enough.

**Session wrap-up:** run `/handoff` (project skill) to write the durable handoff — it updates *this doc* + `DECISIONS.md` at a clean phase boundary, or drops a mid-work cursor into gitignored `docs/HANDOFF.local.md`.
