# Dependencies
node_modules/
.pnp
.pnp.*

# Build / framework output
dist/
build/

# SQLite databases (local substrate store — never commit)
*.db
*.db-shm
*.db-wal
.output/
.vinxi/
.tanstack/
.wrangler/

# Env & secrets — never commit
.env
.env.local
.env.*.local

# Local aggregated data (personal — never commit, when it exists)
src/data/the data file

# Session handoff scratch — mid-work resume cursor written by /handoff.
# Local-only; the canonical state is START-HERE.md + DECISIONS.md.
docs/HANDOFF.local.md

# Optional local reference snapshot at repo ROOT (kept out of git).
# Anchored with a leading slash so it does NOT also ignore docs/reference/
# (the committed, distilled studied-template teardown that CLAUDE.md points to).
/reference/

# OS / editor
.DS_Store
*.log
.vscode/
.idea/

# Kept local-only — excluded from the public repo (build-in-public = the Agent OS build itself)
/CLAUDE.md
/tasks/
/.claude/
/docs/reference/
