# START HERE — agent-os

> New session (human or AI)? Read this first. It's the fastest path to *"I know what's going on."*
> You should **not** need to re-read the whole repo to be productive — this doc + `DECISIONS.md` are the state.

## What this project is
Jarod's own **Agent OS**: a local-first personal control plane that **acts** on his AI stack — see + control all agents / skills / tools / context in one place — with a shared **memory/brain** for all his agents. Greenfield, for his own daily use. Inspired by, **not** a port of, a studied agent-OS template (distilled in local notes, kept out of the repo).

## Intent anchor (north star, working form)
> A local Agent OS that **acts** on my stack — one place to see and control all my agents, skills, tools, and context — with a shared **memory/brain** as the piece I most want.

**Canonical North Star: [`STRATEGY.md`](../STRATEGY.md)** (formalized 2026-07-01). The quote above is the working-form restatement.

## The vision so far
**Three buckets of functionality** — all in scope, none cut:
- **A. Observe + Control** — see every agent/skill/tool/context across runtimes, and *act* on them (install / toggle / run / hand off). The "acts, not observes" core.
- **B. Daily prescription** — a Dream-style audit that prescribes *and runs* the highest-leverage actions.
- **C. The Agent Brain** — a shared 4-layer memory across all agents (Jarod's headline interest — see `MEMORY-SYSTEM-VISION.md`).

**The architecture that serves all buckets** (this *is* "architecting around the north star"):
- **Seam #1 — typed data contract** (one zod schema, producer + consumer).
- **Seam #2 — runtime/agent abstraction** (one adapter interface every runtime implements).
- **Shared-state substrate** — a local **MCP Gateway** (all runtimes point at it; hosts the brain-as-MCP-server) + a **Task Board** (blackboard coordination). The brain = shared state behind the gateway.

**Scope decided: "Rung 2"** — a *local* control plane on one machine, but with those two seams built **remote-ready**, so driving agents on other machines / a VPS later ("Rung 3") is an *extension*, not a rewrite.

## Where we are RIGHT NOW
- ✅ **Understand + Design + Migrate + `ce-strategy`** (2026-07-01) — the reference study distilled, feature slate + memory vision written, North Star canonical in [`STRATEGY.md`](../STRATEGY.md).
- ✅ **`ce-brainstorm`** (2026-07-01) — slice 1 scoped: the **"parity-enabling cut"** (Brain/board substrate seed, agents-primary + 4-runtime inventory + parity actions). Decisions #9 (slices sequence, never shrink) and #10 logged.
- ✅ **`ce-doc-review` ×2** — requirements hardened by a 6-reviewer panel (8 fixes), plan hardened by a 5-reviewer round-2 panel (11 findings walked through and applied: capture-time redaction, token model, in-process capture, session-keyed handoffs, U13/U14/U15).
- ✅ **`ce-plan`** (2026-07-01) — **implementation-ready plan: [`docs/plans/2026-07-01-001-feat-slice-1-substrate-parity-plan.md`](plans/2026-07-01-001-feat-slice-1-substrate-parity-plan.md)**. Forks closed by decision #11: thin vertical slice · Claude Code + Codex writes · **Brain seed is the v1 flagship**.
- ✅ **Whole-system architecture pass** (2026-07-02, decisions #12–13) — closed a mid-`ce-work` gap: no consolidated whole-system view existed, so execution kept wobbling (3 step-backs in one session). Produced the durable cohesion map → **[`ARCHITECTURE.md`](ARCHITECTURE.md)** (Jarod affirmed the exit condition). Adopted **4 extensibility hooks** (per-run cost/tokens, Event Spine, `Inferred<T>` provenance, Brain redaction choke-point) + **3 reserved homes** (Notifications, Economics, Scheduling). **Roster updated:** OpenClaw dropped; real set = Claude · Codex · Hermes · Cursor · Antigravity · OpenCode. **Runtime = Bun confirmed**; FE framework deferred to U11 (lean: React Router 7 / Remix, Tailwind + Shadcn, colorblind-safe). Ideas mined from the studied template → local notes (kept out of the repo).
- ✅ **`ce-work` begun — U13 gate GO + U1 shipped** (2026-07-02). U13 test #2 validated the sharpest assumption (raw-trail resume-sufficiency): a *cold* agent reconstructed in-flight state + the exact next action from a mechanically-extracted trail alone (decision #14). Test #1 (injection) was skipped — mechanism already de-risked, cadre confounded. **U1 committed** (`2bd3a14`): Bun scaffold + typed contract core (seam #1) — all 5 records + all 4 hooks (#13); **30 tests green, tsc clean**. Stack + roster locked (decision #15).
- ✅ **Slice-1 substrate MERGED to `main`** (PR #1, 2026-07-04; merge `f0c7727`). U1 typed contract (seam #1) **+ U14 config-write discipline engine** (`src/configwrite/*`): byte-exact backup → merge-don't-clobber → write-to-temp + atomic rename → **identity-checked** undo journal; format-aware (JSON/TOML/YAML); backups `0600` under the OS data dir (KTD6 / R11). Full review loop ran — `ce-work` → `ce-simplify-code` → `ce-code-review` → **3 Codex adversarial passes** → Copilot (folded in, `68826ba`). U1 contract hardened alongside: `maxSensitivity` redaction escalation, **`WorkState` as a discriminated union** so its resume-state invariant holds at the exported JSON-Schema boundary (not just zod parse), `strictObject` records. **65 tests green, `tsc` clean.** Two config-write robustness findings **deferred** (decision #16) — now tracked as `deferred` issues.
- ✅ **U2 — store + persistence MERGED** (PR #3, 2026-07-04; merge `71c123d`). `src/store/*`: 6 Drizzle tables on `bun:sqlite` (WAL), **async repo interface** (Turso swap point, KTD3), `readWorkState` derives the `WorkState` discriminated union (AE1/AE2), self-recording access-log + `hitRate` (an explicit primitive, NOT auto-logged). Full loop + bot review. 5 residuals tracked (open-findings U2-R1…R6); **U2-R1 is a live instruction for U4**: validate external HTTP/MCP writes at the repo boundary via the contract schema.
- ✅ **U3 — server spine + security COMPLETE, in PR #4** (awaiting bot re-review + merge; HEAD `e524ebf`). `src/server/*` + `src/paths.ts`: one Bun+Hono process; `securityGate` = loopback (socket-peer) + Host allowlist + per-boot CSPRNG token (timing-safe); **fail-closed routing** (gate default-on via `use("*")`, `/health` the one exemption); loopback-only `127.0.0.1` bind; `0700` data dir; bind-then-write + atomic token; dataDir single-instance lock. Redaction + `/work-state` **deferred to U4** (decision #17). Loop ran: ce-work → simplify → ce-code-review → **3 autonomous Codex adversarial passes** (found fail-closed routing, token-clobber, single-instance — all folded). **125 tests green, tsc clean.** Single-instance `flock` hardening deferred to U15 (decision #18, U3-R3).
- ✅ **U4 — Brain MCP server + tools + redaction MERGED** (PR #6, merge `16aae66`). The flagship payoff — **the substrate is agent-consumable.** `src/redact/*` (the schema-driven redaction choke-point, KTD2 — masks at/above `secret`, escalates per-record via `maxSensitivity`, fail-closed, shape-preserving), `src/workstate/*` (the ONE shared `read→redact→present→log` path — the `read_work_state` tool and `GET /work-state` route are byte-identical by construction), `src/mcp/*` (`@modelcontextprotocol/sdk@1.29` WebStandard transport, transport-per-session on `mcp-session-id`, 3 tools), all behind the U3 gate. **Identity model (decision #19):** sessionId = transport session id (KTD8); machineId = a **persisted opaque UUID** (not `os.hostname()` — bot review caught that it bakes the user's name into every record); source = validated self-declared input. **Resolved U2-R1/R5/R6.** Full loop: ce-work → ce-simplify → **ce-code-review (5 personas — security CLEAN BILL: no secret escapes any read path)** → **Codex adversarial (cross-model — caught a session-cap TOCTOU + drove the pager to keyset `(ts,id)`)** → CodeRabbit/Copilot (folded: machineId PII → opaque id, array-mask shape, machine-id validation). **164 tests green, tsc clean.** Deferred: **U4-R1** (hitRate mixes session-id producers — DORMANT, `/status` discards the value).
- ✅ **U5 — Claude Code breadcrumb capture (raw lane) MERGED** (PR #8, merge `dc16e10`; decision #20). `src/capture/tailer.ts` (harness-agnostic byte-offset tailer — Codex/U8 reuse it via the injected `Extractor`) + `src/capture/claude-code.ts` (the ONE module holding the undocumented JSONL shape). **Sufficiency gate = GO:** the spike (5 messy mid-implementation cuts, closed-book, opus — full report `docs/spikes/2026-07-05-u5-resume-sufficiency.md`) validated the core bet AND set the breadth: the trail recovers *direction* but not *observations*, so U5 also captures a terse **`toolUseResult` digest** (test pass/fail, errors, subagent verdict — structural, survives crashed/terse sessions). Capture-time secret classifier (KTD2) feeds the U4 redactor; breadcrumb `sessionId` = CC transcript id (U4-R1 documented, deferred). Full loop: ce-work → ce-simplify → ce-code-review (correctness/security/adversarial/testing — **security caught a real classifier boundary bug**: `\b` misses `DATABASE_PASSWORD=`) → **Codex cross-model gate ×2** (caught a no-lost-crumbs seam violation; the in-place-rewrite residual was **overridden as a scoped defer to U8** — CC is append-only, so the invariant is narrowed to "append-only inputs"). **229 tests green, tsc clean.** Deferred: **U5-R1** (uncapped cold-start read), **U5-R2** (rewrite safety → U8).
- ✅ **Repo went public + housekeeping** (2026-07-05, decision #22) — scrubbed ALL studied-template association (name + `docs/reference/` + FEATURE-SLATE gone from tree **and** history), pushed to a fresh public **[`jarodtaylor/agent-os`](https://github.com/jarodtaylor/agent-os)**, archived the pre-public repo private (`agents-os-internal` — PRs + issues preserved), added the README front door, migrated the 17 open issues (roadmap + deferred) to the public tracker. **Zero-association verified across tree + history + remote.**
- ⏳ **Next: `ce-work` U6 — Claude Code consumption + clean-end hooks** (`hooks/*`, `src/install/claude-code.ts`; deps U3, U4, U5, U14; plan §U6). SessionStart hook (matcher `startup|resume|clear`) reads the token file at call time, calls the gated `GET /work-state`, and emits `hookSpecificOutput.additionalContext` with the work-state summary + lane/freshness; SessionEnd records a graceful-end marker (NOT load-bearing — U5 owns capture). Installer registers hooks in `~/.claude/settings.json` AND the brain MCP server in `~/.claude.json` (two files by design), both via the U14 config-write engine. **This is the felt checkpoint — a fresh CC session consuming the substrate deterministically.** ⚠️ **U3-R3 trigger FIRES here:** U6 is the first real token consumer, so the real `flock` lock (U15) must land no later than U6 — see the flock-lock finding (U3-R3, `deferred`). Same loop (ce-work → ce-simplify → ce-code-review → codex gate → PR). KTDs 1–9 + the 4 hooks are constraints. Reference: `docs/ARCHITECTURE.md`.

## Read next (in order)
1. `docs/plans/2026-07-01-001-feat-slice-1-substrate-parity-plan.md` — THE plan (scan headings: Goal Capsule → unit index → U13).
2. `docs/DECISIONS.md` — decisions 1–22 logged; 3 forks still open (deployment, remote adapter, memory routing).
3. `docs/MEMORY-SYSTEM-VISION.md` — design grounding.
4. `docs/spikes/` — the validation spikes (e.g. the U5 resume-sufficiency test that gated the raw lane).

## Open threads
- ✅ **NotebookLM connected** (2026-07-01) — `notebooklm` CLI authenticated + skill installed (`~/.claude/skills/notebooklm`); 11 notebooks verified. Memory-relevant: **"AI Second Brain"** (`ba4b1af4…`), **"AgentOS"** (`0db2b38d…`), **"Claude, Hermes, and NotebookLM"** (`7d55c036…`). This unlocks the v1.1 Brain deepening + the memory-routing fork — pull the sources when the memory architecture phase starts (not during slice-1 build).
- **U12 spike** owns the non-Claude/Codex lane discovery — Hermes (9119 write surface) + Cursor/Antigravity/OpenCode as they come online — findings land in DECISIONS.md.
- **3 remaining deferred forks** (deployment model, first remote adapter, memory routing) — see `DECISIONS.md` open table.
- **Codex hit-rate** is the measured bet: if AGENTS.md-pointer consumption is weak, investigate Codex `features.hooks` in v1.1.
- **Tracked residuals + roadmap → [GitHub Issues](https://github.com/jarodtaylor/agent-os/issues)** (decision #21): deferred findings carry the `deferred` label; upcoming units carry `roadmap`. **Skim the open issues at session start** (`tasks/open-findings.md` is retired, local-only). Still-hot triggers: the **flock-lock** finding (U3-R3) must land no later than **U6** (its first token consumer); **U5-R1** (cold-start read) lands with U6/boot wiring; **U5-R2** (rewrite safety) → U8.
- **Repo is public: [`jarodtaylor/agent-os`](https://github.com/jarodtaylor/agent-os)** — a build-in-public reference/journey, **not** a supported OSS product. The pre-public history (PRs #1–9 + the GitHub issue tracker) lives in the private archive **`agents-os-internal`** (renamed 2026-07-05); internal/personal files (entry-point instructions, `tasks/`, `.claude/`, local reference notes) stay untracked + gitignored, kept local. A public **README** is the front door (next polish).
- **Codex adversarial review is autonomous now** — `~/.claude/scripts/codex-adversarial-review.sh` wraps the openai-codex companion (the slash command is `disable-model-invocation`). It's a required loop step; see [[jarod-compound-engineering-pipeline]] + `tasks/lessons.md`.

## Continuity note
Migrated from an earlier local project on 2026-07-01. The original Claude Code session that produced all this may still be open as a fallback backstop. If anything here is unclear, that session is the deep backup — but this doc + `DECISIONS.md` should be enough.

**Session wrap-up:** run `/handoff` (project skill) to write the durable handoff — it updates *this doc* + `DECISIONS.md` at a clean phase boundary, or drops a mid-work cursor into gitignored `docs/HANDOFF.local.md`.
