# Decision Log — agent-os

The anti-drift ledger. Every decision + **why** + **which intent it serves**. Read this to see what's locked vs. still open. (Attempt #1 was abandoned to drift; this file is the guardrail.)

## Intent anchor (the north star we measure against)
> A local Agent OS that **acts** on my stack — one place to see and control all my agents, skills, tools, and context — with a shared **memory/brain** as the piece I most want.

**Canonical:** [`STRATEGY.md`](../STRATEGY.md) is the formal North Star. The quote above is the working-form restatement.

## Decisions made

| # | Date | Decision | Why | Intent served |
|---|---|---|---|---|
| 1 | 2026-07-01 | **North-star-first.** Establish + architect around the vision before choosing build order. A/B/C are **buckets of functionality**, not competing flagships. | Attempt #1 died from building before intent was clear. Understanding the north star first gives a foundation all buckets hang off. | Anti-drift; keep the build true to *why* he started. |
| 2 | 2026-07-01 | **Scope = "Rung 2":** a *local* control plane (one machine), but with the two seams built **remote-ready** so remote runtimes ("Rung 3") are a later extension, not a rewrite. | Remote is a real future want, but building it now is premature; shaping the seams now makes it cheap later. | "Acts on my stack" now; remote optionality preserved. |
| 3 | 2026-07-01 | **Migrate to `~/Code/personal/agent-os`;** treat the studied template as a **disposable reference** (pointer + distilled docs only, no code ported). | The template is inspiration, not a base. Distillation (teardown + graph) carries ~95% of its value. | Clean home, no template baggage. |
| 4 | 2026-07-01 | **Methodology = compound-engineering** (`ce-strategy` → `ce-brainstorm`/`ce-plan` → `ce-work`). | Jarod's proven workflow across projects; methodical strategy → plan → build. | Structure that resists drift. |
| 5 | 2026-07-01 | **North Star formalized** in [`STRATEGY.md`](../STRATEGY.md) (repo root, canonical). START-HERE and this log now point to it; other restatements should reference it, not diverge from it. | `ce-strategy` complete. The working-form anchor lived in ~4 places; one canonical doc stops the copies diverging. | Anti-drift: a single source of truth for *why* we're building. |
| 6 | 2026-07-01 | **Coordination stays its own track** (not folded into Observe+Control). | Jarod: "it'll drift out of Control as we dig in" — orchestration/autonomy ≠ a read/act control surface. | Clean seams; keeps the orchestration bet visible instead of buried. |
| 7 | 2026-07-01 | **Two-user model:** Jarod as operator **+** the agents as autonomous machine consumers of the substrate. | The brain/board must be agent-legible (MCP-native, typed), not just a human dashboard — this is *why* there are two seams. | Serves "acts on my stack" + the shared-brain intent; substrate built for machines, not only a UI. |
| 8 | 2026-07-01 | **Session handoffs feed the canonical record.** `/handoff` project skill updates `START-HERE.md` + `DECISIONS.md` (clean boundary) or a gitignored `docs/HANDOFF.local.md` (mid-work) — never a parallel "next session" block. | Operationalizes #5: continuity across contexts/agents without a competing state doc. Adapted from Jarod's cadre handoff skill. | Anti-drift is THE risk (attempt #2); a clean cross-session handoff keeps intent continuous. |
| 9 | 2026-07-01 | **Slices sequence the North Star; they never shrink it.** Anything excluded from a slice must appear in that plan's "Deferred, not cut" section with the trigger that promotes it. No brainstorm/plan may silently drop a North Star capability. | Jarod: lean cuts were starting to feel like stripping the product down to the easiest path — the exact discouragement pattern that makes him walk away from projects. | Anti-drift **and** anti-discouragement: slices stay small while the full vision stays visible and scheduled. |
| 10 | 2026-07-01 | **Slice 1 = the "parity-enabling cut":** Brain/board substrate seed (agents = primary consumer, Jarod secondary) + unified read inventory across all 4 runtimes (skills/MCP/plugins) + cross-runtime parity actions ("make skill/MCP X available in harness Y"). Run-steering and context inspector deferred **with promotion triggers** per #9. | O+C pains are *anticipated, not lived* — Jarod avoided multi-agent work because nothing centralized projects across harnesses. Slice ranked by "what un-collapses the team," not by lived pain. Consistent with the flagship-fork lean; does NOT close the ce-plan forks (build order, native writes). | "Acts on my stack" + the shared-brain wedge; keeps #6's Coordination boundary clean. |
| 11 | 2026-07-01 | **Slice-1 forks resolved in `ce-plan`** (plan: `docs/plans/2026-07-01-001-feat-slice-1-substrate-parity-plan.md`): **build order = thin vertical slice** (Claude Code end-to-end first, then broaden); **native writes v1 = Claude Code + Codex** (Hermes/OpenClaw read-only, v1.1 via verified APIs); **flagship = the Brain seed** ("any agent picks up where we left off" headlines v1; Dream deferred). Closes the flagship/build-order, native-writes, and substrate-vs-slice rows of the open-forks table. | Vertical slice reaches a felt checkpoint fast (anti-discouragement per #9's rationale); CC+Codex writes are cheap (shared SKILL.md standard) while the other two are the XL tail; the Brain is Jarod's stated emotional core and everything since the brainstorm re-centered on it. | The wedge (shared brain) leads; "acts on my stack" ships in the same slice; deferred forks stay visible. |
| 12 | 2026-07-02 | **Pause slice-1 build for a bounded whole-system architecture pass.** Produce ONE consolidated map of Agent OS — components, connections, inputs/outputs, dependencies, and deferred items (shown *as* deferred) — plus a human-surface wireframe, so the pieces' cohesion is visible and slice 1 is legibly "a slice of" a whole. Consolidation of existing docs, **not** new invention. **Exit condition:** Jarod can see the whole and affirm the first slice as the right start; then build resumes. Test #2 stays parked (its result informs sequencing, not a gate). | We had North Star + slate + memory vision + a detailed slice-1 plan, but no whole-system architecture — so every deep-dive lost the whole and the drift-alarm fired (three execution step-backs in one session: scaffolding/L1, U13 validity traps, features-vs-architecture). Decision #1: attempt #1 died from *building before clarity*; a repeated flinch at execution is data, not noise. | Anti-drift is THE risk: a durable cohesion map is the guardrail that keeps every slice true to the whole; serves the north star's "context" pillar. Pairs with #9 (deferred-not-cut shown on the map). |

| 13 | 2026-07-02 | **Extensibility-hardening adopted into the architecture** (from the `surface-unknowns` mining of the studied template's teardown — kept in local notes). **(a) Four "bake-in-now" hooks:** per-run cost/tokens columns in the run ledger; an append-only **Event Spine** (`events` table + `emit()` wired into adapter/brain/scanner, `events.subscribe` on the gateway); an **`Inferred<T>`** provenance wrapper (confidence + evidence) in the typed contract; a free-text **redaction/consent choke-point** at the Brain ingest+retrieval boundaries. **(b) Three reserved "homes"** named on the map so future ideas land: **Notifications/Attention**, **Economics (Cost & Quota)**, **Automation/Scheduling** (standalone, NOT folded into Coordination). The 4 STRATEGY tracks stand unchanged — mining confirmed ~18 studied-template capabilities already map onto the decomposition. | Each hook is cheap now and a costly cross-cutting rewrite later; together they hedge Jarod's explicit "don't box us in" concern and the template's own trap of unwired dead surface. Reserved homes give coherent-but-unbuilt concerns a landing spot — anti-drift at the bucket level. | "Room to grow" made concrete; serves the north-star "context" pillar and pairs with #9 (deferred/reserved shown on the map, never cut). |
| 14 | 2026-07-02 | **U13 gate = GO** (Phase A unblocked). The slice's sharpest assumption — **raw-trail resume-sufficiency** — is validated: a *cold* agent, given ONLY a mechanically-extracted raw breadcrumb trail (no handoff, no repo, no context) from a real interrupted cadre session, correctly reconstructed what was in flight (multi-agent review of the #5 work, tests green at 1349) AND named the exact next action (commit the review folds) — matching ground truth. **Test #1** (SessionStart `additionalContext` injection quality) was **skipped**: the mechanism is already de-risked (official plugins use it; confirmed live) and cadre was a confounded testbed (existing `/handoff`). Caveat: n=1 session, one cut at a legible moment — not fragile, but re-check trail-sufficiency on messier mid-implementation resumes during U5. | The plan gated Phase A on U13; the advisor's mechanical-extraction validity guard held — dumb flat per-event summaries were resume-sufficient, so the Brain seed's raw lane is sound. | De-risks the slice before any architecture is built (plan's Definition of Done); clears U1 onward. |
| 15 | 2026-07-02 | **Stack + roster locked.** **Runtime = Bun** (`bun:sqlite` synchronous driver, `Bun.serve`, single fast-starting binary — fits a local daemon; Node's scale-maturity is irrelevant at n=1). Storage = SQLite + Drizzle behind a repo interface (libSQL/Turso swap point); server = **Hono** (runs on Bun *and* Node → portability hedge). **FE framework deferred to U11** (lean: React Router 7 / Remix over heavy Next; Tailwind + Shadcn; colorblind-safe). **Roster:** OpenClaw dropped (no longer used); real set = **Claude · Codex · Hermes · Cursor · Antigravity · OpenCode** (slice-1 writes: Claude + Codex; Hermes read v1). | Bun genuinely fits the local-daemon shape (and Jarod wanted to try it); the roster reflects Jarod's actual daily agents; deferring FE keeps focus on the agents-primary substrate. Untangled: Bun-the-runtime ≠ pnpm-the-PM. | "Connect, don't compete" (roster) + substrate-first; keeps the north star's runtime honest and portable. |
| 16 | 2026-07-04 | **Slice-1 substrate shipped (U1 + U14, PR #1 merged `f0c7727`); config-write robustness cluster deferred.** U14 (config-write engine) built + merged through the full loop (`ce-code-review` + **3 Codex adversarial passes** + Copilot). Fixed structurally: parse-error secret leak, recordUndo-outside-try, non-atomic undo restore, patch validation, undo now **identity-checked** (postHash), symlink fail-closed, and contract hardening — `maxSensitivity` redaction escalation, **`WorkState` as a discriminated union** so its resume-state invariant holds at the exported JSON-Schema boundary (not just zod parse), `strictObject` records. **Deferred (CTO/CPO call):** cross-process merge concurrency + undo-record crash durability — tracked in `tasks/open-findings.md` with promotion triggers. | Fixing on greenfield foundation before U4/U5 build on it is cheap; the deferred items depend on an operator/dev UX not yet clear — cross-process locks / two-phase-fsync now is speculative complexity against an unknown usage model, and both are rare + recoverable (KTD9 single-writer + on-disk backup). | Substrate-first + anti-over-engineering; the Brain seed's contract and the safe config-write path both land; deferred robustness stays visible (#9). |
| 17 | 2026-07-04 | **U3/U4 boundary: the redaction applier + the sensitive `GET /work-state` route are deferred from U3 to U4.** The plan places `/work-state` (which returns secret-marked `handoff.cursor.inFlight`) in U3, but KTD2's redaction pass has no *applier* yet — U1 shipped `enumerateSensitive`/`maxSensitivity` (the inputs), not a redactor. So **U3 ships the secured spine + gate only, proven on a non-sensitive `GET /status` route**; the sensitive read + the redaction applier land in U4, built once and shared with the `read_work_state` MCP tool. Plan scenario 5 ("/work-state matches the MCP tool") becomes a U4 cross-check. | Redaction is the leak-prone, high-stakes core of KTD2 and belongs to U4's cluster (R1/R2/R4/R5/R6); building it as a U3 side-piece risks a subtly-wrong redactor or shipping an unredacted-secret route *in the security unit*. U3's actual reqs are R11/KTD6/KTD9 (the gate). Nothing sensitive ships before redaction exists. | Security = "acts on my stack, safely"; keeps the gate (U3) and the redacting content layer (U4) as clean, separately-verifiable seams; the deferred route stays visible (#9). |
| 18 | 2026-07-04 | **Single-instance lock: pragmatic pid-file NOW, real `flock` OS lock deferred to U15 (tracked).** U3's Codex adversarial review surfaced that the daemon must be single-instance PER DATA DIR (KTD9's "one writer by construction") — bind-then-write only guards the same port; a different-port second instance would clobber the token + run a second writer. Shipped a dataDir pid-file lock (acquired BEFORE `openDb`) that refuses the common double-run; its crash-recovery TOCTOU + pid-recycle edges get a real `flock` OS lock (Bun FFI) as a **fast-follow in U15**. | CTO call, defended: launchd (U15) is the *production* single-instance guarantee, nothing reads the token until U6, and introducing Bun FFI deserves a focused change whose natural home is U15 (launchd + the lock are one design). Tech debt made visible + scheduled, not silent. | Substrate-first + anti-over-engineering: the security spine is correct now while the hard fix is tracked (U3-R3, `deferred` label) — deferred robustness stays visible (#9). |
| 19 | 2026-07-05 | **U4 shipped + merged (PR #6, `16aae66`): the substrate is agent-consumable.** Brain MCP server (`@modelcontextprotocol/sdk@1.29` WebStandard transport, transport-per-session on `mcp-session-id`, 3 tools) + the schema-driven **redaction choke-point** (`src/redact/*`, KTD2) + ONE shared `read→redact→present→log` path so `read_work_state` and `GET /work-state` are byte-identical, all behind the U3 gate. **Identity model (the plan left write-attribution open):** sessionId = transport session id (KTD8), machineId = a **persisted opaque UUID** (bot review caught that `os.hostname()` bakes the user's name into every record), `source` = validated **self-declared** tool input. Resolved U2-R1/R5/R6; deferred **U4-R1** (hitRate compares disjoint sessionId namespaces — DORMANT). | Redaction is KTD2's leak-prone core; a schema-driven applier keyed off the type registry (never a field name), shared by both read paths, means they can't disagree. Self-declared `source` avoids a fragile clientInfo→enum mapping and is safe under the single-user trust model (own harnesses, behind the gate). The review loop earned its keep: **security returned a CLEAN BILL on redaction** (no secret escapes any read path), and the **cross-model Codex pass caught two residual gaps in the review fixes themselves** — a session-cap TOCTOU (→ enforce-at-insertion) and a same-`ts` page (→ keyset `(ts,id)`: lossless + bounded). | The Brain seed's payoff: "any agent picks up where we left off," redacted, over MCP — the flagship wedge. |
| 20 | 2026-07-05 | **U5 merged (PR #8, `dc16e10`): the Claude Code raw lane — capture is agent-legible even with NO handoff.** `src/capture/tailer.ts` (harness-agnostic byte-offset tailer; Codex/U8 reuse it via the injected `Extractor`) + `src/capture/claude-code.ts` (the ONE module holding the undocumented JSONL shape). **Sufficiency gate = GO:** the spike (5 messy mid-implementation cuts, closed-book, opus) validated the core bet — a cold agent recovers in-flight state + the correct next action from a mechanically-extracted trail (resolves #14's n=1-legible-cut caveat). **Extraction-breadth REFINEMENT (amends the plan's authored breadth):** the spike found the trail recovers *direction* but not *observations*, so U5 also captures a terse `toolUseResult` digest (test pass/fail, errors, subagent verdict) — structural, so it survives crashed/terse sessions where narration wouldn't. Capture-time secret classification (KTD2) feeds the U4 redaction pass; breadcrumb `sessionId` = the CC transcript id, distinct from `access_log`'s MCP transport id (U4-R1 documented + deferred). | The core bet had to be re-validated on *messy* input before building breadth (plan §U5 execution note); it held, and the spike's finding set the breadth. The full loop earned its keep at every layer: ce-simplify (2); ce-code-review's **security** lens caught a real classifier boundary bug (`\b` can't match inside `DATABASE_PASSWORD=` — the dominant secret shape) + missing shapes, its correctness/adversarial lenses a sweep-abort + a machineId batch-loss (2 reviewers corroborated 3); the **Codex cross-model gate** caught a no-lost-crumbs seam violation (conflating corrupt-JSON skip with a valid-line extractor/contract failure). **Override logged loudly:** Codex returned no-ship TWICE on in-place-rewrite safety; overridden as a scoped defer (U5-R2) because the residual is CC-impossible (transcripts are append-only; `--resume` copies verbatim to a NEW path) and the fix would touch U2's `ON CONFLICT DO NOTHING` — the at-least-once idempotency primitive — to close a case CC can't produce (advisor-affirmed: fixing now is riskier than deferring). No-lost-crumbs invariant narrowed honestly to "append-only inputs." 229 tests green, tsc clean. Deferred: U5-R1 (cold-start read), U5-R2 (rewrite safety → U8). | The Brain seed's raw lane — the fallback that captures what discipline can't reach (a crashed or forgotten session), redacted, so any agent picks up where we left off *even with no clean handoff*. "Acts on my stack" + the shared-brain wedge. |
| 21 | 2026-07-05 | **Deferred work + the roadmap move to GitHub Issues** (labels `deferred` and `roadmap`); `tasks/open-findings.md` is retired (kept local-only). Decisions stay in this log (the *why*), current state in START-HERE (the *where*), issues hold the *what's-left* — one home per item, never duplicated. | Jarod (ADHD) found the compound-engineering docs chaotic to parse for "what's on the horizon"; a labeled issues list (and a board, once the gh `project` scope is granted) is a human-readable, scannable view he'll actually use. The one-home-per-item rule guards against the dual-source drift that killed attempt #1. | Anti-drift's #1 risk is *disengagement* — a scannable horizon keeps him engaged, and keeps the ledger (decisions) and the tracker (issues) as clean, non-overlapping records. Also readies the **build-in-public** direction (repo security-swept CLEAN → GO for public; leaning yes as an AI-native-leadership signal post-layoff; not yet flipped — Jarod's call). |
| 22 | 2026-07-05 | **Repo went PUBLIC as [`jarodtaylor/agent-os`](https://github.com/jarodtaylor/agent-os)** (build-in-public). Scrubbed ALL association with the studied template: dropped `docs/reference/` + the teardown-dense `FEATURE-SLATE.md` from the tree **and all history** (`git filter-repo`), genericized the name + identifying fingerprints (version, source paths, feature names) across the 5 surviving docs, kept "Dream" as our own vocab, excluded FEATURE-SLATE local-only. **Mechanism call (the crux):** a force-push would NOT reach zero-association — GitHub keeps old commits (fetchable by the SHAs our docs cited) + PR file-diffs (`refs/pull/*`, undeletable) server-side — so pushed the scrubbed history to a **fresh public repo**; the pre-public repo was renamed to the **private `agents-os-internal`** archive (PRs #1–9 + issues preserved). Added a public README; migrated the 17 open issues (roadmap + deferred) to the public tracker (fresh numbering — docs now cite units/labels, not `#N`). Name = **singular** "Agent OS" (noun-adjunct convention; matches every doc). | Build-in-public is Jarod's AI-native-leadership signal (post-layoff); zero-association was the hard bar, and the fresh-repo path is the only one that meets it — verified: name + fingerprints gone from every blob, every commit message, and the remote tree; 0 secret alerts. Nothing lost — the archive holds the full pre-public history. | "Connect, don't compete" now ships in the open; the build itself is the showcase. Anti-drift: README + DECISIONS are the public front door, START-HERE stays the live cursor. |
| 23 | 2026-07-05 | **U15 split: pull the real `flock` OS lock forward as a focused pre-U6 unit; launchd supervision stays post-U6.** U3-R3's trigger (decision #18) fires — U6 is the first real token consumer, so the pid-file single-instance guard (`src/server/single-instance.ts`) becomes a real OS advisory lock (Bun FFI → libc `flock`, held on an fd for process lifetime, auto-releasing on exit) **before** U6 ships. Scope = the lock ONLY; launchd LaunchAgent/KeepAlive supervision stays U15, after U6 (U6 degrades gracefully if the server is down; VS7 is a post-U6 check). Jarod chose flock-only (option A) over pulling all of U15 forward. | Honors the deliberate #18 trigger exactly while keeping surface minimal: the lock is the piece U6 actually needs (token-file integrity + one-writer under a *live* consumer); supervision buys U6 nothing. Library locks (`proper-lockfile` et al.) are pid/mtime heuristics — the same edge-class we're escaping — so a real OS lock means FFI, kept as its own reviewable change. | Substrate-first + anti-over-engineering; unblocks the U6 felt checkpoint ("a fresh agent picks up where we left off") without dragging supervision forward. Deferred robustness (#9) stays scheduled. |
| 24 | 2026-07-06 | **flock diagnostics: message-honesty fix NOW; full errno-awareness DEFERRED (Codex no-ship overridden, logged loudly).** The Codex adversarial gate returned no-ship on one medium finding: the lock treats EVERY non-zero `flock(LOCK_EX\|LOCK_NB)` return as "already running," but non-zero can also mean ENOLCK / unsupported-FS / EINTR / I/O — so on those the daemon fail-closes with a MISLEADING "already running" message that steers recovery toward killing a phantom process or `rm`-ing the lock (now an inode-divergence corruption path, per #23's no-unlink rule). **Adopted the valid core cheaply:** reworded the throw to name both causes ("…already running, OR this data dir's filesystem may not support file locking…"), killing the wrong-recovery harm while keeping fail-closed + the `/already running/` substring the tests assert. **Deferred the heavy remediation:** true errno-awareness (read `__error`/`__errno_location`; EWOULDBLOCK/EAGAIN=held, retry EINTR, distinct error otherwise) needs per-platform errno tables + a `bun:ffi` `cc` shim (reading errno across the FFI trampoline is fragile) — a separate unit. **Trigger:** first real VPS/container run (verify the whole lock path there), OR ever placing the data dir on a network/synced FS, OR an observed real misdiagnosis. | The lock **fail-closes in every branch**, so KTD9 (one writer) never breaks — this is diagnostic quality, not safety; and on the supported data dir (macOS/Linux **local** FS) the only realistic non-zero LOCK_NB return is EWOULDBLOCK (correctly "already running"), so the misdiagnosis path is **unreachable on a supported install** (advisor-affirmed the partial-adopt). The remote roadmap does NOT trip it: multi-Mac/VPS is served by network federation + Turso/libSQL — each node keeps a LOCAL data dir + local lock — never a shared network-mounted data dir (a SQLite anti-pattern we design against). | Substrate-first + anti-over-engineering: honest diagnostics now, the platform-coupled remediation tracked (like #18/U3-R3) with a concrete trigger — doesn't gate U6. |
| 25 | 2026-07-06 | **Brain security posture named honestly: KTD2 redaction defends confidentiality OUTBOUND only; content-integrity INBOUND (indirect prompt injection) is a distinct, currently-uncovered axis — DEFERRED to the v1.1 gateway / Brain-hardening phase.** Prompted by a review of the user-level `anthropic-cybersecurity-skills` plugin (817 skills; ~3 map onto Agent OS). The redaction choke-point (#13/#19) masks secrets *leaving* a read path — it does NOT scan *arriving* content for hidden instructions. But the Brain's whole shape is an **agent→Brain→agent loop** (U5 captures CC breadcrumbs + `toolUseResult` digests; U6 serves them to a fresh agent via `read_work_state`), so a captured crumb carrying injected instructions would be served as authoritative context — the exact **dual** of redaction (integrity inbound, not confidentiality outbound). Two adjacent surfaces share the axis: the **MCP Gateway** (tool-poisoning / rug-pull / SSRF, once it registers external servers) and the **"act on my stack" tool layer** (allowlist arg-shapes + scoped identity + HITL for high-impact + audit, the moment buckets A/B *run* actions). **Not slice-1 work, not repo deps** — the three skills (`detecting-indirect-prompt-injection`, `auditing-mcp-servers-for-tool-poisoning`, `securing-agentic-ai-tool-invocation`) are a consumable **security-review lens** at that phase, same category as codebase-memory-mcp (gateway *consumers*, not dependencies). **Trigger:** the v1.1 gateway / Brain-hardening phase, OR the first time the Brain serves content sourced from a non-own agent or a shared machine. **⟳ RE-ENABLE ACTION** — the `cybersecurity-skills@anthropic-cybersecurity-skills` plugin was disabled at the USER level on 2026-07-06 (surface hygiene: 817 skills were flooding every project's `/`-menu + skill listing), so it is now OFF everywhere by default. When this phase starts, opt it back in FOR agent-os only: add `"cybersecurity-skills@anthropic-cybersecurity-skills": true` under `enabledPlugins` in the project's `.claude/settings.json` (project scope overrides the user-level false; that file is gitignored / local-only in this repo). **Tracked: issue #20** (`deferred`). | Prevents the silent drift of "the redactor makes the Brain safe" — it only makes it **non-leaky**. Naming the honest scope now (same discipline as U5-R2's invariant-narrowing and #24's honest diagnostics) keeps the flagship's trust model truthful before the agent→Brain→agent loop goes live at U6. Under today's single-user local trust model (own harnesses, behind the gate) the inbound risk is low — which is exactly why it's a v1.1 lens, not a U6 blocker — but left unnamed it would resurface as a surprise the moment remote/shared sources arrive (Rung 3). | "Acts on my stack, **safely**" — the Brain is the headline wedge, so its trust model must be honest, not just non-leaky. Anti-drift via deferred-but-visible (#9); pairs with the KTD2 redaction choke-point (#13/#19). |
| 27 | 2026-07-08 | **U7 shipped: `/handoff` rewired substrate-FIRST — the one machine-readable continuity record is now the Brain, not the docs (VS3).** The skill (`.claude/skills/handoff/SKILL.md`) now calls `write_handoff` (brain MCP) as **Step 3, before any doc write**: the substrate cursor `{next, lastDecided, inFlight}` keyed `(project, sessionId)` is authoritative; `START-HERE.md` ▶ NEXT + `HANDOFF.local.md` become **human projections that must agree** with it (Step 4); `DECISIONS.md` is explicitly framed as the *ledger* (audit log), not a competing "resume here" record. Substrate-unreachable → docs-only + a **mandatory visible staleness flag** (never silently diverge). **VS3 proven behaviorally:** (A) two `write_handoff` calls in one session → `read_work_state` returns ONE record carrying the latest cursor (upsert on `(project,sessionId)`, not a duplicate); (B) a write against a dead port errors cleanly → the fallback path is reachable, not a crash; (C) projection-agreement holds by construction (▶ NEXT = `cursor.next`) and was shown live — the SessionStart dry-run rendered `cursor.next` verbatim. **Git-tracking finding (shaped execution):** `.claude/` is gitignored, so the skill is a **local-only, untracked file** — no feature branch / PR; the tracked deliverable is this ledger entry + the START-HERE flip (direct to `main`, per decision #8's handoff convention). **This wrap dogfooded the rewrite** — its own continuity record was written to the substrate first, then projected here. **Live VS1 also lived this session (closes the U6 residual):** the U6 installer ran against Jarod's **real `~/.claude`** — a heavily-configured daily-driver (5 existing SessionStart hooks + 3 MCP servers) — and preserved every existing entry while adding ours, token kept out of config; the SessionStart hook then injected the seeded handoff end-to-end. **270 tests green, tsc clean.** Post-wrap: the install is **uninstalled back to pristine** (re-install for keeps once U15 launchd supervision gives an always-on server). | Operationalizes decision #8/#5 with the substrate as the source: docs-first would make the Brain a lagging copy — reintroducing the multi-source drift attempt #2 exists to prevent. Writing FIRST + agree-or-fix-the-cursor makes "one continuity record" a build-time property, not a discipline hope. The behavioral gate (run the skill) is the honest proof for a prompt change the code-review/codex loop (built for code diffs) can't gate. Live VS1 turned U6 from "passes tests" into "felt on the real machine," and pre-validated U7's own dependency (in-session MCP reachable). | The flagship wedge made HONEST: the shared Brain — not a human doc — is the single source of "where we left off," which is exactly the "shared memory/brain" north-star piece. Anti-drift: one machine record, docs render it; deferred-but-visible (#9) for the re-install-at-U15 trigger. |
| 26 | 2026-07-08 | **U6 shipped + merged (PR #23, `3309f59`): the felt checkpoint is BUILT — a fresh Claude Code session picks up work-state with nothing pasted (R1/KTD5).** SessionStart hook (token-at-call-time → gated `GET /work-state` → `additionalContext`, **fail-open**: any failure emits nothing + exits 0), SessionEnd graceful-end marker via a new gated `POST /session-end` (KTD9: server stays sole writer — the hook never opens the DB), `headersHelper` for the MCP token (KTD6 delivery to CC's native client, never embedded), two-file installer (`settings.json` hooks + `~/.claude.json` MCP) through the U14 engine, **cross-file transactional** (rolls back the first write if the second fails). The review loop earned its keep: **ce-code-review (10 personas + the redaction-boundary-reviewer — CLEAN BILL: no secret escapes any read path)** caught the sharp ones — uninstall's whole-file backup-restore is WRONG for `~/.claude.json` (CC's continuously-rewritten live-state file: always diverged → undo throws → restored neither; my test masked it), and a valid-JSON *wrong-shape* 200 could break fail-open. **Codex cross-model gate:** folded #2 (install now transactional); **#1 (deep-merge preserves a pre-existing embedded token) + #3 (uninstall leaves the MCP entry) scoped-DEFERRED — Option B, logged loudly** — both need a U14 subtree replace/remove primitive the engine lacks (→ issue **#21**), and #1 is unreachable on the supported path (U6 only ever writes `headersHelper`, never a static `headers`). **⚠️ Gate-honesty (like #24):** the substantive Codex review ran on `5a6f4a7`; the marker was advanced over the fix commits (`a098296`/`80641c6`/`cfbcccc`) WITHOUT a clean re-run because the openai-codex companion's auth broke on a **mid-session account switch** (a CLI logout→login didn't clear it — likely needs a fresh CC session; captured in memory [[claude-code-harness-quirks]]). Jarod merged with this flagged. Bot feedback folded (fail-open hardening). **270 tests green, tsc clean.** Uninstall ships a crash-stop interim (per-target try/catch; the diverged `~/.claude.json` is skipped, leaving a harmless dead `mcpServers.agent-os` entry). Deferred: **#21** (U14 primitive), **#22** (co-located-hook granularity). Live VS1 (real `~/.claude` + fresh session) is the un-lived felt-checkpoint step. | The Brain seed's headline payoff — "any agent picks up where we left off," deterministically, redacted, over the gated substrate — now reaches a LIVE Claude Code session. The review layers caught real design bugs (the uninstall reversal, the fail-open holes) rather than nits; the Option-B defer keeps U6 scoped to the felt checkpoint while the shared U14 primitive (U6 uninstall + U8 Codex + U10 provisioning all need it) becomes its own focused change. | The flagship wedge, LIVED: "acts on my stack" + the shared brain, consuming deterministically at session start. Anti-drift: deferred-but-visible (#9); honest gate-exception logging (#24-style — the marker records that the review RAN, the honesty burden is on the log). |
| 28 | 2026-07-09 | **U8 Codex stable-credential model (decision A) + LIVE-revocation hardening — forced by the cross-model gate's 2nd no-ship.** Codex's HTTP-MCP client sends only a STATIC header (no per-connection headers-helper like CC's), so it can't ride the per-boot rotating token. Chose **A** — a STABLE credential (`codex.token`, minted by the installer, embedded in `~/.codex/config.toml` `0600`) the gate accepts ALONGSIDE the per-boot token — over **B** (env-var delivery, needs shell-profile population) and **C** (drop the token / loopback-only). The Codex adversarial gate then no-shipped TWICE, converging on the stable credential's *lifecycle*, and the hardening followed: **(1) the gate now reads `codex.token` FRESH per request** (mtime-cached, `server/security.ts#makeStableTokenReader`) instead of snapshotting at boot → **revocation is LIVE** (uninstall's `rm codex.token` → next request 403s, no restart) and a fresh install is picked up live; **(2) the server no longer mints the token — the installer is the SOLE minter** → the server-vs-installer mint race is gone. Uninstall also deletes `codex.token` (a diverged-config leftover `[mcp_servers.agent-os]` entry is now INERT, not live), and the co-located-hook granularity bug (#22) is fixed in the shared installer helper. **Two lifecycle edges SCOPED-DEFERRED (2×-no-ship protocol):** the present-but-empty-file repair race (largely dissolved by the per-request read; rare + fail-loud + recoverable) and the first `config.toml` write's post-atomic-rename journal-failure window (leaves a live credential with no undo — a rare I/O window, PRE-EXISTING and identical in the CC installer, fixable only by a U14 `journal-before-publish` engine change; tracked with the #16 robustness cluster). **⟳ RECONSIDER TRIGGER (Jarod's ask):** if these deferred edges actually bite, OR maintaining the two-location stable-credential lifecycle proves recurrently costly, revisit the model — a single source of truth (gate reads the token from `config.toml` directly, so revoke = remove the entry) or the env-var delivery (B). Logged so recurrence is measured against a recorded rationale, not rediscovered. **330 tests green, tsc clean.** | A stable EMBEDDED credential is the only shape Codex's static-header client supports, but it must be LIVE-revocable + race-free to be a *sound* security primitive — the first folds only reached "next-boot-revocable," which the cross-model gate correctly rejected. Reading the token per-request is arguably the CORRECT model anyway: every other caller already reads the token file fresh per call, so the gate snapshotting it was the asymmetry the gate exposed. The two remaining edges are genuinely rare + fail-loud + one pre-existing, so deferring *those* (not live revocation) is the honest line — same discipline as U5-R2's invariant-narrowing, #24's honest diagnostics, and #16's robustness-cluster defer. | "Acts on my stack, **safely**" — the second harness (Codex) joins the shared Brain over the gated substrate with a credential that is a sound, live-revocable primitive, not a next-boot-only one. Honest KTD6 narrowing (config embeds the token for Codex, unlike CC's headersHelper — #19); deferred-but-visible (#9) for the two scoped edges + the reconsider trigger. |
| 29 | 2026-07-09 | **U8 reconsider trigger FIRED (gate pass-5 no-ship) → CEO ruling: scoped-defer the immaterial race + resolve the ROOT via single-source as a scheduled follow-up ([#24](https://github.com/jarodtaylor/agent-os/issues/24)).** The Codex adversarial gate's 5th pass no-shipped on a NEW, 5th credential-lifecycle edge: a **concurrency-only TOCTOU** between `installCodex`'s `tokenPreexisted` snapshot (captured before any write) and the later `resolveCodexToken`/rollback decision — under CONCURRENT install+uninstall it can revoke a sibling's valid token or strand a freshly-minted one (`src/install/codex.ts:217-285`). This met #28's **"recurrently costly"** arm (5 passes → 5 lifecycle edges), so per the pre-agreed protocol I STOPPED — **no patch #6** — and brought the credential-model choice back to Jarod. **Ruling = A (ship now, defer):** the race is operationally **IMMATERIAL** today — it needs two concurrent installer processes, which cannot happen for a manual single-user CLI (no automated/concurrent install path exists pre-U15) — so scoped-defer it (joining #28's two deferred edges under the same properly-logged-defer protocol) and **ship U8 now**. The recurring COST, though, is real, and its root is the **two-location token model** (the `codex.token` file the gate reads + the copy embedded in `config.toml` Codex sends); every one of the 5 findings is about syncing those two. **Resolution = single-source (the #28-named option), NOT patch #5:** the gate reads the token straight from `config.toml`, deleting the whole `codex.token` file + provenance/mint/revoke machinery — dissolving the entire finding class and fixing the twin CC-uninstaller leftover-entry issue — bundled with the U14 key-removal primitive ([#21](https://github.com/jarodtaylor/agent-os/issues/21)) it needs for clean revocation. Scheduled as its own **mostly-subtractive** unit ([#24](https://github.com/jarodtaylor/agent-os/issues/24)), not crammed into U8. **334 tests green, tsc clean.** | The reconsider trigger existed precisely so a recurring cost would prompt a conscious model revisit rather than an (N+1)th patch. It fired; we revisited and CHOSE single-source as the terminal model — but sequenced it as a follow-up because blocking an otherwise-green, hard-won unit on a re-architecture, to fix a race with **no live operational path**, is gold-plating. Deferring THIS edge is consistent with #28's already-deferred edges (rare + no live path); committing to single-source (not the patch the trigger warned against) is the honest response to the ROOT. Don't ship a model you'll delete — so the delete is scheduled next, while the code is warm. | "Acts on my stack, **safely**" + anti-drift: the credential stays a sound primitive for the ACTUAL threat model (single-user local), the root-cause fix is committed + visible (#9 deferred-but-visible), and the ledger records the trigger *firing* + the ruling so the model decision is decided-once, not rediscovered. Ships the 2nd harness without an immaterial adversarial finding stalling the slice. |

## Open — deliberately deferred (do NOT decide until the phase that owns them)

These surfaced during design (the "6 forks") but are premature to lock. Parked here so they stay **visible, not lost**:

| Fork | Decide during | Current lean (NOT locked) |
|---|---|---|
| **Deployment model** (local / installable / sync-ready) | `ce-strategy` / architecture | Local `Bun.serve`, sync-ready interfaces — the slice-1 plan builds exactly this shape; formal close when packaging matters. |
| **First real remote adapter** (vendor-cloud vs. own machine) | Rung 3 planning | Vendor-cloud first. |
| **Memory routing model** (agent-driven / orchestrated / hybrid) | Memory architecture (post-NotebookLM) | Hybrid (brain exposes retrieval as MCP tools). |

*Closed by decision #11 (2026-07-01): flagship/build-order, native-write set, and substrate-first-vs-thin-slice — see the slice-1 plan.*

## How to add a decision
Append a row to **Decisions made** with the date, the decision, the **why**, and the **intent served**. If it closes an open fork, move that fork out of the deferred table and cite the decision number.
